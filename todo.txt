priority:

play inconsistencies still?
    - fixed by using ytdlp nightly visionos, changing ytdl vid client to visionos for non-song vidoes
    - could still fail, e.g. 2/280 in my playlist failed to fetch/download
x/p, y/p.
analyze how cookie file is actually used / what we actually need
update/refactor help screen




other:

oauth credentials are broken rn, use browser auth. switch to oauth when fixed
discord rich presence
queue reordering
queue unshuffle (unlikely / doesn't even make sense)
custom window layouts, to make better use of hjkl idea
dont autoplay on app open, wait for space (?)
volume standardizer?
remove toasts, and have a dedicated status/pending message location (left of Vol?)
lyrics
color themes (simple file)
track listing truncation broken not keeping on 1 line for special e.g. cjk chars
add little visual indicating a song is downloaded? (e.g. a '↓' symbol at end of line)
v/V vim highlight commands to select lines in a row/block, x,d on that etc